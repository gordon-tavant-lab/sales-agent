# Capabilities — tvt-sales-agent

> Stub (T0). Full inventory filled in at T12 once the roster (T1) and factory (T9) are built.

## Entry point

- `tvt-sales-agent` — dynamic dispatcher, single entry point for the sales/intel/pm/create family.

## Vendored skill family (24)

See `VENDORED_FROM.md` for the full list + source provenance. 22 roster-family skills
(intel/sales/pm/create) + `tvt-tavantize` + 2 governance dependencies (`tvt-gov-guard`,
`tvt-gov-attest`).

## Agents

- `agents/` — empty until the first promotion (see `plan.md` §4/§5).
